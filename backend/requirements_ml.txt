# Install AFTER requirements.txt succeeds.
# These are large (1-3 GB) and only needed for ML model training/inference.

# PyTorch CPU-only (much smaller than GPU version)
# Run: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.3.0

# YOLOv8 damage detection
ultralytics==8.2.0

# OpenCV (use headless version on servers)
opencv-python==4.9.0.80

# ONNX runtime for fast inference
onnxruntime==1.18.0
